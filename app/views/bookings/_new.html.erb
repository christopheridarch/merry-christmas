  <div class="form-container">
    <h2>Book  <%= @xmas_item.name.capitalize %> à <%= @xmas_item.user.firstname.capitalize %></h2>
    <%= simple_form_for([@xmas_item, @booking]) do |form| %>
      <%= form.input :start_date %>
      <%= form.input :end_date %>
      <div class="text-left">
        <%= form.submit "Check availability", class: "btn btn-danger" %>
      </div>
    <% end %>
  </div>

